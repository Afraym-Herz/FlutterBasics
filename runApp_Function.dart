import 'package:flutter/material.dart';
/// main function is the entiry point to Dart code include flutter apps 
void main (){ 
  runApp( 
    ///  runApp()' is a Flutter-specific method that initializes the app and connects the widget tree to the screen.
   
    ///  we write all app's code in the runApp function.
    
    ///  widget tree. In this example, the widget tree consists of two widgets, the Center widget and its child the Text widget.
     
    ///  The framework forces the root widget to cover the screen, which means the text “Hello, world” ends up centered on screen. 
  )
  }
